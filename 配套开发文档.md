# AlgoAscend 配套开发文档

> 2026年"中国软件杯"大学生软件设计大赛 A3 赛题参赛作品  
> 出题方：科大讯飞 | 赛题：基于大模型的个性化资源生成与学习多智能体系统开发

---

## 一、项目概述

### 1.1 项目背景

传统高等教育教学中，所有学生面对统一的教学大纲、统一的教材、统一的PPT和统一的试卷。然而，学生的知识基础、认知风格、学习节奏千差万别，"一刀切"的教学模式难以适应个性化学习需求。特别是在计算机专业的核心课程——数据结构与算法中，抽象概念多、思维方式转变大，是公认最需要因材施教的领域。

AlgoAscend 以此为切入点，构建了一套基于大模型的个性化资源生成与学习多智能体系统，目标是为每个学生提供一个"懂自己的AI学习伙伴"。

### 1.2 项目简介

**AlgoAscend** 是一个专为 C++ 算法竞赛学习打造的多智能体 AI 学习平台。系统基于 **CrewAI 多智能体协作框架**，设计了7个专业 AI Agent（学习画像分析师、内容生成专家、内容审核员、练习设计教练、学习路径规划师、智能辅导老师、学习评估分析师），通过对话式交互构建六维学习画像，动态生成个性化学习资源（PPT、思维导图、代码案例、动画视频、AI数字人讲解等），规划渐进式学习路径，并提供实时智能辅导答疑。

### 1.3 赛题对照

| 赛题要求 | AlgoAscend 实现 |
|----------|----------------|
| 学习画像构建 | 对话式六维画像（知识基础、认知风格、学习节奏、薄弱点、兴趣偏好、学习目标），自动提取+置信度评估 |
| 个性化资源生成 | 6种资源类型：PPT课件、思维导图、代码实操案例、实践项目框架、Manim算法动画、AI数字人讲解视频 |
| 学习路径规划 | 7阶段渐进路线，从基础语法到高级算法竞赛，支持动态调整 |
| 智能辅导答疑 | 多模态解答：文字+流程图(Mermaid)+公式(KaTeX)+动画(Manim)+数字人，逐步引导式辅导 |
| 学习评估反馈 | 多维度评估报告，自动调整学习路径，首页智能推荐刷新 |
| 讯飞工具集成 | 讯飞星火大模型(多模型)、讯飞文生图API、讯飞AI虚拟人API |
| 拓展阅读 | Hello Algo知识库集成，个性化推荐 |

---

## 二、技术架构

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────┐
│                    前端 (React 18)                    │
│  ChatPage │ DashboardPage │ ResourcesPage │ ...      │
│  Tailwind CSS │ Zustand │ Canvas 2D │ Mermaid │ KaTeX│
├─────────────────────────────────────────────────────┤
│                   FastAPI 后端 (Python)               │
│  /api/chat │ /api/profile │ /api/resources │ ...     │
│  安全中间件 │ SSE流式输出 │ 内容过滤                   │
├─────────────────────────────────────────────────────┤
│              CrewAI 多智能体框架 (7 Agents)            │
│  Orchestrator → Profile → Content → Review →         │
│  Exercise → Path → Tutor → Assessment                 │
├─────────────────────────────────────────────────────┤
│         外部AI服务 (多模型支持)                        │
│  讯飞星火 X2/Pro/Ultra │ DeepSeek │ OpenAI │ Kimi ... │
│  讯飞文生图 TTI │ 讯飞AI虚拟人 VMS2D                  │
└─────────────────────────────────────────────────────┘
```

### 2.2 技术选型

| 层级 | 技术 | 版本 | 选型理由 |
|------|------|------|----------|
| 前端框架 | React + Vite + TypeScript | 18/5/5.5 | 组件化开发、HMR热更新、类型安全 |
| UI框架 | Tailwind CSS | 3.4 | 原子化CSS、深色主题支持好 |
| 状态管理 | Zustand | 4.5 | 轻量、无boilerplate、支持持久化 |
| 图表渲染 | Mermaid.js | 11.16 | 文本驱动、支持多种图表类型 |
| 数学渲染 | KaTeX | 0.17 | 快于MathJax、支持LaTeX |
| 代码高亮 | react-syntax-highlighter | 15.5 | 多语言支持 |
| 后端框架 | FastAPI | 0.115 | 异步高性能、自动OpenAPI文档 |
| ORM | SQLAlchemy | 2.0 | 成熟稳定、支持SQLite |
| 多智能体 | CrewAI | 1.15 | Python原生、支持工具调用 |
| HTTP客户端 | httpx | ≥0.28 | 异步支持、兼容requests API |
| 数据库 | SQLite | - | 零配置、适合单机部署 |

### 2.3 多智能体设计

系统采用 **CrewAI 框架**实现多智能体协作，设计了7个专业Agent：

| Agent | 角色 | 核心能力 | System Prompt 要点 |
|-------|------|----------|-------------------|
| ProfileAgent | 学习画像分析师 | 对话式提取六维特征 | 从对话中提取知识基础/认知风格/学习节奏等信息 |
| ContentAgent | 内容生成专家 | 生成多模态教学资源 | 按画像定制PPT/思维导图/代码案例 |
| ReviewAgent | 内容审核员 | 防幻觉、学术准确性 | 审核生成内容的正确性和安全性 |
| ExerciseAgent | 练习设计教练 | 针对性出题 | 根据易错点生成选择/填空/算法设计题 |
| PathAgent | 学习路径规划师 | 个性化路线设计 | 7阶段渐进式路线，含里程碑 |
| TutorAgent | 智能辅导老师 | 多模态答疑 | 主动生成Mermaid图、逐步引导 |
| AssessmentAgent | 学习评估分析师 | 动态评估调整 | 跟踪掌握度、识别薄弱点 |

Agent 之间通过 CrewAI 的 Task/Process 机制协作，由 Orchestrator 根据用户意图自动路由到合适的 Agent 组合。

---

## 三、功能模块详解

### 3.1 对话式学习画像构建

**技术实现**：ProfileAgent 在首次对话中通过自然语言对话提取学生的知识基础、学习目标、薄弱环节等六维特征，沉淀到 `StudentProfile` 表。后续对话持续更新画像，置信度随交互次数提升。

**关键代码**：`backend/agents/profile_agent.py`、`backend/config.py` 中的 `PROFILE_DIMENSIONS`

### 3.2 多模态资源生成

系统支持6种资源类型的自动生成：

| 资源类型 | 生成方式 | 关键依赖 |
|----------|----------|----------|
| PPT课件 | LLM生成Markdown结构 → python-pptx渲染 | backend/routes/resources.py |
| 思维导图 | LLM生成Markdown大纲 → Mermaid.js渲染 | frontend/components/MindmapRenderer.tsx |
| 代码实操案例 | LLM生成带注释的C++完整代码 | POST /api/resources/generate-code-case |
| 实践项目框架 | LLM设计综合性算法项目 | POST /api/resources/generate-project |
| Manim算法动画 | LLM生成Manim Python脚本 → manim渲染为MP4 | 后端调用manim CLI |
| AI数字人讲解 | LLM生成讲稿 → 讯飞虚拟人API驱动 → ffmpeg录制 | POST /api/resources/generate-xfyun-digital-human |

### 3.3 智能辅导答疑

TutorAgent 根据问题类型自动选择最佳解答形式：
- **算法流程** → 自动生成 Mermaid 流程图
- **数理推导** → KaTeX 数学公式渲染
- **代码问题** → 语法高亮的逐步讲解
- **抽象概念** → Manim 动画可视化

### 3.4 学习路径规划

**7阶段渐进路线**：
1. C++ 基础语法回顾
2. 基础数据结构（数组/链表/栈/队列）
3. 树与图基础
4. 基础算法（排序/搜索/贪心）
5. 动态规划入门到进阶
6. 高级数据结构（线段树/并查集/Trie）
7. 竞赛综合训练

每个阶段含里程碑检查，完成评估后自动解锁下一阶段。

### 3.5 评估闭环

评估分析师定期触发评估，基于对话历史、练习记录、路径完成情况生成多维度报告。评估结果自动触发三项操作：
1. **路径调整**：薄弱环节增加时间分配，已掌握内容跳过
2. **推荐刷新**：Dashboard "为你推荐"区域实时更新
3. **画像更新**：置信度和知识维度刷新

### 3.6 竞赛平台集成

通过后端代理绕过CORS，支持四大竞赛平台数据查询：
- **Codeforces**：rating、比赛记录、题目分类
- **AtCoder**：rating、参加记录  
- **洛谷 (Luogu)**：AC数量、rating
- **牛客网 (Nowcoder)**：技能树

---

## 四、数据库设计

### 4.1 ER图（核心表）

```
Student (学生)
  ├── id, name, email, created_at
  │
  ├── StudentProfile (六维画像)
  │     ├── student_id (FK)
  │     ├── knowledge_level (知识水平 1-10)
  │     ├── cognitive_style (认知风格)
  │     ├── learning_pace (学习节奏)
  │     ├── weak_points (薄弱点 JSON)
  │     ├── interests (兴趣偏好 JSON)
  │     ├── learning_goals (学习目标 JSON)
  │     └── confidence (置信度 0-1)
  │
  ├── ChatMessage (对话记录)
  │     ├── student_id (FK)
  │     ├── role (user/assistant)
  │     ├── content
  │     └── timestamp
  │
  ├── LearningResource (学习资源)
  │     ├── student_id (FK)
  │     ├── type (ppt/mindmap/code_case/project/video)
  │     ├── title, content, file_path
  │     └── created_at
  │
  ├── LearningPath (学习路径)
  │     ├── student_id (FK)
  │     ├── current_stage (当前阶段)
  │     ├── stages (阶段列表 JSON)
  │     └── updated_at
  │
  ├── Exercise (练习记录)
  │     ├── student_id (FK)
  │     ├── type (choice/fill/algorithm)
  │     ├── question, answer, student_answer
  │     └── is_correct
  │
  └── Assessment (评估记录)
        ├── student_id (FK)
        ├── dimensions (各维度得分 JSON)
        ├── suggestions (改进建议 JSON)
        └── created_at
```

### 4.2 数据库选型

使用 **SQLite** 的理由：
- 零配置，无需安装数据库服务
- 评审老师可直接运行，无需额外环境
- 单文件存储，便于提交
- SQLAlchemy ORM 提供抽象层，未来可无缝迁移到 PostgreSQL

---

## 五、API 接口文档

### 5.1 对话接口

**POST /api/chat/send** — 发送消息（SSE流式响应）

```
请求体:
{
  "message": "我该如何学习动态规划？",
  "student_id": 1,
  "stream": true,
  "model": "spark-x2",
  "api_key": "...",
  "api_base": "https://spark-api-open.xf-yun.com/x2/chat/completions",
  "api_model": "spark-x"
}

响应: SSE事件流
data: {"type": "token", "content": "动态规划是..."}
data: {"type": "done"}
```

**GET /api/chat/history/{student_id}** — 获取对话历史

**DELETE /api/chat/history/{student_id}** — 清空对话历史

**POST /api/chat/completions-proxy** — OpenAI兼容代理透传（处理讯飞等CORS受限提供商）

### 5.2 画像接口

**GET /api/profile/{student_id}** — 获取六维学习画像

```
响应:
{
  "knowledge_level": 6,
  "cognitive_style": "visual",
  "learning_pace": "moderate",
  "weak_points": ["动态规划", "图论"],
  "interests": ["算法竞赛", "C++"],
  "learning_goals": ["系统掌握DP", "参加竞赛"],
  "confidence": 0.85
}
```

### 5.3 资源接口

**POST /api/resources/generate-ppt** — 生成PPT课件  
**POST /api/resources/generate-mindmap** — 生成思维导图  
**POST /api/resources/generate-code-case** — 生成代码实操案例  
**POST /api/resources/generate-project** — 生成实践项目框架  
**POST /api/resources/generate-manim** — 生成Manim算法动画  
**POST /api/resources/generate-xfyun-digital-human** — 生成讯飞AI数字人视频  
**GET /api/resources/recommend/{student_id}** — 获取个性化推荐

### 5.4 平台接口

**POST /api/platform/fetch** — 代理查询竞赛平台数据

```
请求体:
{
  "platform": "codeforces",
  "handle": "tourist",
  "endpoint": "user_info"
}
```

### 5.5 其他接口

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/agents` | GET | 列出所有智能体 |
| `/api/dashboard/{id}` | GET | 仪表盘数据 |
| `/api/knowledge/search?q=` | GET | 知识库搜索 |
| `/api/assessment/evaluate` | POST | 触发学习评估 |

---

## 六、部署说明

### 6.1 环境要求

| 依赖 | 版本要求 |
|------|----------|
| Python | 3.10+ |
| Node.js | 18+ |
| npm | 9+ |
| 操作系统 | Windows 10+ / macOS / Linux |

### 6.2 一键启动（Windows）

```bash
# 双击项目根目录下的 启动.bat
# 脚本自动完成：环境检测 → 依赖检查 → 构建前端 → 启动服务 → 打开浏览器
```

### 6.3 手动部署

```bash
# 1. 配置环境变量
cp .env.example .env
# 编辑 .env，填入 API Key

# 2. 安装后端依赖
cd backend
pip install -r requirements.txt

# 3. 构建前端
cd ../frontend
npm install
npm run build

# 4. 启动服务
cd ../backend
python main.py
# 访问 http://localhost:8000
```

### 6.4 配置说明

`.env` 文件关键配置项：

```ini
# LLM提供商配置（支持多模型）
LLM_PROVIDER=deepseek
LLM_API_KEY=your_api_key
LLM_API_BASE=https://api.deepseek.com
LLM_MODEL=deepseek-chat

# 服务配置
PORT=8000
DATABASE_URL=sqlite:///./learning_platform.db
```

**注意**：系统支持在设置页面中动态切换多个模型提供商，包括讯飞星火（X2/Pro/Ultra/Max/Lite）、DeepSeek、OpenAI、Kimi、GLM、Qwen、Claude 等，无需修改配置文件。

---

## 七、项目结构

```
AlgoAscend-Web/
├── 启动.bat                      # Windows 一键启动脚本
├── .env.example                  # 环境变量模板
├── README.md                     # 项目说明
├── LICENSE                       # MIT 许可证
├── NOTICE.md                     # 第三方依赖声明
│
├── backend/                      # 后端代码
│   ├── main.py                   # FastAPI 入口（CORS、中间件、路由注册）
│   ├── config.py                 # 配置：Agent System Prompt、画像维度
│   ├── models.py                 # SQLAlchemy ORM 模型（7张表）
│   ├── database.py               # 数据库会话管理
│   ├── llm_service.py            # 统一LLM调用服务（流式/非流式/JSON）
│   ├── requirements.txt          # Python 依赖
│   ├── agents/                   # 多智能体系统
│   │   ├── orchestrator.py       # 原生编排器（关键词路由+SSE）
│   │   ├── crewai_agents.py      # CrewAI Agent 定义（含工具调用）
│   │   ├── crewai_orchestrator.py # CrewAI 编排器
│   │   ├── profile_agent.py      # 学习画像分析师
│   │   ├── content_agent.py      # 内容生成专家
│   │   ├── exercise_agent.py     # 练习设计教练
│   │   ├── path_agent.py         # 学习路径规划师
│   │   ├── tutor_agent.py        # 智能辅导老师
│   │   └── assessment_agent.py   # 学习评估分析师
│   ├── routes/                   # API 路由
│   │   ├── chat.py               # 对话接口（SSE流式+代理透传）
│   │   ├── profile.py            # 画像接口
│   │   ├── resources.py          # 资源生成接口（6种资源类型）
│   │   ├── assessment.py         # 评估接口
│   │   ├── knowledge.py          # 知识库接口
│   │   ├── problem_bank.py       # 内置题库（300+题）
│   │   ├── scrape.py             # 爬虫接口
│   │   └── platform.py           # 竞赛平台代理接口
│   ├── middleware/__init__.py     # 内容安全中间件
│   ├── hello_algo_kb.json        # Hello Algo 知识库
│   ├── runoob_kb.json            # 菜鸟教程知识库
│   └── generated/                # 生成的文件（Manim脚本/PPT等）
│
├── frontend/                     # 前端代码
│   ├── package.json              # 依赖配置
│   ├── vite.config.ts            # Vite 构建配置
│   ├── tailwind.config.js        # Tailwind CSS 配置
│   ├── tsconfig.json             # TypeScript 配置
│   ├── index.html                # 入口 HTML
│   ├── src/
│   │   ├── App.tsx               # 主应用（路由+粒子效果+侧边栏）
│   │   ├── main.tsx              # ReactDOM 入口
│   │   ├── index.css             # 全局样式
│   │   ├── components/           # 组件
│   │   │   ├── Sidebar.tsx       # 侧边栏导航
│   │   │   ├── ChatMessage.tsx   # 对话消息卡片
│   │   │   ├── MarkdownRenderer.tsx # Markdown+Mermaid+KaTeX渲染
│   │   │   ├── RadarChart.tsx    # 六维雷达图（Canvas）
│   │   │   ├── KnowledgeGraph.tsx # 知识图谱
│   │   │   ├── AgentPipeline.tsx  # Agent协作可视化
│   │   │   ├── ResourceGenerator.tsx # 资源生成器（多Tab）
│   │   │   ├── SettingsModal.tsx  # 模型设置（三大分类）
│   │   │   ├── OrbitParticleRing.tsx # 粒子轨道环特效
│   │   │   └── ...
│   │   ├── pages/                # 页面
│   │   │   ├── ChatPage.tsx      # 智能对话页（图片上传+模型切换）
│   │   │   ├── DashboardPage.tsx # 学习仪表盘（为你推荐+评估按钮）
│   │   │   ├── ResourcesPage.tsx # 学习资源（拓展阅读卡片）
│   │   │   ├── ProfilePage.tsx   # 学习画像
│   │   │   └── PathPage.tsx      # 学习路径
│   │   ├── services/api.ts       # API封装（MODEL_REGISTRY+多模型支持）
│   │   └── stores/useStore.ts    # Zustand 全局状态
│   └── dist/                     # 构建产物（生产环境静态文件）
│
└── 演示视频脚本.md                # 演示视频脚本
```

---

## 八、安全设计

### 8.1 API Key 管理

- API Key 通过设置页面输入，存储在浏览器 localStorage
- 支持多模型独立配置，每个模型可设置不同的鉴权字段
- 后端不存储任何第三方 API Key，仅在使用时从前端传入

### 8.2 内容安全

- 后端中间件对用户输入进行敏感词过滤
- AI 生成内容通过内容审核员 Agent 检查
- 输出限制在学术讨论范围，拒绝非学习相关的请求

### 8.3 数据安全

- SQLite 数据库存储在本地，不涉及网络传输
- 画像数据带置信度评分，避免过度推断
- 支持对话历史一键清空

---

## 九、许可证

本项目采用 MIT 许可证开源。第三方依赖声明见 `NOTICE.md`。

---

## 十、团队分工（模板）

| 角色 | 职责 |
|------|------|
| 队长 | 系统架构设计、多智能体框架搭建 |
| 后端开发 | API接口开发、数据库设计、CrewAI集成 |
| 前端开发 | UI设计与实现、交互逻辑、可视化组件 |
| AI开发 | Agent Prompt设计、模型调优、多模态集成 |
| 文档/测试 | 技术文档编写、功能测试、演示视频制作 |
